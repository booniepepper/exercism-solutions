name: nth-prime
version: 2.1.0.6

dependencies:
  - base

library:
  exposed-modules: Prime
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - primes

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - nth-prime
      - hspec
